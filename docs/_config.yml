remote_theme: "mmistakes/minimal-mistakes@4.28.1"
plugins:
  - jekyll-include-cache
defaults:
  # _pages
  - scope:
      path: ""
      type: pages
    values:
      layout: single
      sidebar:
        nav: "docs"