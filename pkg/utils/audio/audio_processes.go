package audio

// Processes stores the processes that can be done on audio files
type Processes string

const (
	// Resample defines the process of resample the wav
	Resample Processes = "Resample"
)
