name: Run tests and upload coverage

on: [push, pull_request]

permissions:
  contents: read

jobs:
  test:
    name: Run tests and collect coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      # beep/speaker → oto needs ALSA headers for CGO on Linux
      - name: Install ALSA (Linux CGO)
        run: sudo apt-get update && sudo apt-get install -y libasound2-dev

      - name: Install dependencies
        run: go mod download

      - name: Run tests
        # tests/ are external packages (*_test); -coverpkg is required to attribute
        # coverage to the production packages they exercise (see just coverage).
        run: >
          go test -tags "test noaudio"
          -coverpkg=./pkg/...,./internal/...
          -coverprofile=coverage.txt
          ./tests/...
        env:
          CGO_ENABLED: "1"

      - name: Upload results to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: coverage.txt
